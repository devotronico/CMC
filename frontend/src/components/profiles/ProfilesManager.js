import React from 'react';
import PropTypes from 'prop-types';

const ProfilesManager = (props) => {
  return <div>Profiles Manager</div>;
};

ProfilesManager.propTypes = {};

export default ProfilesManager;
